import { ScreenPlaceholder } from "@/components/ScreenPlaceholder";

export default function RulesPage() {
  return (
    <ScreenPlaceholder
      title="Rules"
      blurb="One maker quotes a two-way price on a team's supremacy; everyone else buys or sells. Settles on the score after extra time (120'), penalty shootout = draw. Stakes scale by stage."
      comingIn="Launch"
    />
  );
}
