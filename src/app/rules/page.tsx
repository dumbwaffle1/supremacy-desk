import { ScreenPlaceholder } from "@/components/ScreenPlaceholder";

export default function RulesPage() {
  return (
    <ScreenPlaceholder
      title="Rules"
      blurb="How the market works: one maker quotes a two-way price on home supremacy, everyone else buys or sells. Settles on the score after extra time (120'), penalty shootout = draw."
      comingIn="Prompt 9 / launch"
    />
  );
}
